node_modules/
dist/
.wrangler/
.dev.vars
worker-configuration.d.ts

# scripts/cf-logs.mjs のローカル設定（1Password の参照とアカウント ID）。
# このリポジトリは公開なので、識別子はコミットせず各自の環境に置く。
.cf-logs.env

# フロントエンド（Vite + Svelte）のビルド成果物と依存。
# version.json はデプロイ時に scripts/deploy.sh が frontend/dist/ 内へ生成する
# （稼働コミットの表示用）ため、dist ごと ignore される。
frontend/node_modules/
frontend/dist/

.venv/
__pycache__/
.pytest_cache/

.DS_Store
