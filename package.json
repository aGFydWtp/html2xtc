{
  "name": "url-to-xtc",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:converter": "python3 -m pytest test/converter/"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@cloudflare/workers-types": "^5.20260717.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  }
}
